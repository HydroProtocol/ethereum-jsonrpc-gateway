{
  "_upstreams": "support http, https, ws, wss",
  "upstreams": [
    "http://localhost:8545"
  ],

  "_strategy": "support NAIVE, RACE, FALLBACK",
  "strategy": "NAIVE",

  "_method_limitation_enabled": "limit or not",
  "method_limitation_enabled": false,

  "_contract_whitelist": "can be ignore if the limitation is not enabled",
  "contract_whitelist": [
    "0x..."
  ]
}