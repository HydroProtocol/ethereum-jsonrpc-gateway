stages:
  - build
  - deploy

variables:
  CONTAINER_IMAGE: registry.ddex.io/${CI_PROJECT_PATH}:${CI_COMMIT_SHA}
  CONTAINER_IMAGE_ALIAS: registry.ddex.io/${CI_PROJECT_PATH}:${CI_COMMIT_REF_NAME}

build:
  tags:
    - docker
  image: docker:git
  services:
    - docker:dind
  stage: build
  script:
    - docker build -t $CONTAINER_IMAGE .
    - docker login -u gitlab-ci-token -p $CI_BUILD_TOKEN https://registry.ddex.io
    - docker push $CONTAINER_IMAGE
  only:
    - master

#deploy_ropsten:
#  tags:
#    - docker
#  image: davidqhr/k8s-kubectl:latest
#  variables:
#    KUBECONFIG: /root/.kube-hp/config
#  stage: deploy
#  script:
#    - sed -i "s~<IMAGE>~$CONTAINER_IMAGE~g" .kube.yml
#    - kubectl apply -f .kube.yml --namespace=sandbox
#  only:
#    - sandbox
#
#deploy_production:
#  tags:
#    - docker
#  image: davidqhr/k8s-kubectl:latest
#  variables:
#    KUBECONFIG: /root/.kube-hp/config
#  stage: deploy
#  script:
#    - sed -i "s~<IMAGE>~$CONTAINER_IMAGE~g" .kube.yml
#    - kubectl apply -f .kube.yml --namespace=sandbox
#    - kubectl apply -f .kube.yml --namespace=production
#  only:
#    - master
#  when: manual
